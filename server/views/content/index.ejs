<% include ../partials/header.ejs %>

    <!-- MAIN CONTENT -->
    <div class="container">
      <div class="row">
        <div class="col-md-offset-3 col-md-6">

        <my-app>Loading...</my-app>

          <h1><%= title %></h1>
          <% if(displayName) { %>
            <h2>Welcome <%= title %>, <%= displayName %></h2>
          <% } else { %>
            <h4>Welcome to <%= title %></h4>
          <% } %>
        </div>
      </div>
    </div>

<% include ../partials/angular.ejs %>
<% include ../partials/footer.ejs %>


